# Nothing is bound at /opt/models yet.
#
# /opt/models is the OPTIONAL, read-only bind point for a local GGUF collection.
# Bind a host directory of models to make them available, e.g.
#   podman run … -v /host/models:/opt/models:ro …
#   (distrobox.ini:  volume=/host/models:/opt/models:ro)
# then point ds4-server at a file via /opt/data/ds4.env:
#   DS4_DROSTE_MODEL=/opt/models/<file>.gguf
#
# The easy way to obtain a model: run download_model.sh — it fetches a curated
# DeepSeek V4 quant into the shared HF cache (~/.cache/huggingface) and prints
# the absolute path for DS4_DROSTE_MODEL. No /opt/models bind needed for those.
#
# This file was created to note you have been informed; deleting it re-shows
# the notice at the next start.
